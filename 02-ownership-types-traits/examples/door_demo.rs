// Drive the typestate door through a small scenario.
//
// Run with: `cargo run --example door_demo`

// TODO: import what you need from the `fsm` crate.
// use fsm::{Door, Locked, Unlocked};

fn main() {
    // TODO:
    //   1. Create a locked door with `Door::new()`.
    //   2. Try to unlock with the wrong key — print what happened.
    //   3. Unlock with the right key.
    //   4. Open the door.
    //   5. Lock it again.
    //
    // Note: every transition consumes `self`, so you'll be shadowing the
    // `door` binding (`let door = ...; let door = ...;`) as you go.
    todo!()
}
